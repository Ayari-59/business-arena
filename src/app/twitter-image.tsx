import Image, { alt, contentType, size } from "./opengraph-image";

/** La carte Twitter/X reprend l'image Open Graph : une seule image à tenir. */
export { alt, contentType, size };
export default Image;
